from app import myapp_obj

myapp_obj.run(debug = True)

# For running the Mobile version, use the command given below instead
# myapp_obj.run(debug = True, host = "your computer's IPv4 address")

# When you use the command python3 run.py, a hosting link is generated
# use that hosting link on your mobile device to access the Mobile version
